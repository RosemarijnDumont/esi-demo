# config.py
# This file would contain configuration details, such as database connection strings, API keys, etc.
# For this example, we are using in-memory storage, so this file is minimal.

# Example of a potential configuration for vendor notification (not used directly in main.py for simplicity)
VENDOR_NOTIFICATION_EMAIL = "vendor@example.com"
VENDOR_NOTIFICATION_WEBHOOK = "https://vendor.com/api/notify"
